package com.bah.msd.customerapi;

/*import org.junit.jupiter.api.Test;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.boot.test.context.SpringBootTest;

import com.bah.msd.domain.Customer;
import com.bah.msd.repository.CustomersRepository;

import static org.assertj.core.api.Assertions.assertThat;
*/
/*@SpringBootTest
class CustomersRepoTests {*/

	/*@Autowired
	private CustomersRepository repo;*/

	/*@Test
	void test_addNewCustomer() {

		System.out.println("repository count: " + repo.count());
		assertThat(repo.count()).isEqualTo(1);

		assertThat(repo.existsByName("Michael Scott")).isTrue();
		assertThat(repo.existsByEmail("mscott@dundermifflin.com")).isTrue();

		Customer c = new Customer();
		c.setId(0);
		c.setName("Michael Scott");
		c.setEmail("mscott@dundermifflin.com");
		c.setPassword("password");

		System.out.println("Original c: " + c.toString());

		Customer newCustomer = repo.save(c);

		System.out.println("New c: " + newCustomer.toString());

		System.out.println("repository count: " + repo.count());
		assertThat(repo.count()).isEqualTo(2);

	}*/

